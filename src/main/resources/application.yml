#driver.class.name=org.postgresql.Driver
#db.url=jdbc:postgresql://localhost:5432/javaPro
#db.username=postgres
#db.password=postgres
server:
  port: 8989

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/javaPro
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: none